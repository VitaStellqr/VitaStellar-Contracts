// Auto-generated client for the `crypto_registry` contract used by
// `iot_device_management` to resolve manufacturer signing public keys for
// firmware authentication (see Issue #194).
//
// The client is generated at compile time from the upstream contract
// source via the `contractimport!` macro so the function signature and
// error type stay in sync with the contract without manual maintenance.

soroban_sdk::contractimport!(file = "../../crypto_registry/src/lib.rs");
